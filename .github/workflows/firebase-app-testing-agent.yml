name: Firebase App Testing Agent CI

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main

defaults:
  run:
    working-directory: example

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.x'
          channel: 'stable'

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
            ~/.gradle/caches
          key: ${{ runner.os }}-flutter-${{ hashFiles('example/pubspec.lock', 'pubspec.lock') }}

      - name: Flutter pub get
        run: flutter pub get

      - name: Build Debug APK
        run: flutter build apk --debug --no-tree-shake-icons

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Firebase App Distribution console
        run: echo "Open App Testing results at https://console.firebase.google.com/project/rebase-agent-testing/appdistribution"

      - name: Write Firebase Service Account
        run: echo '${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}' > sa.json

      - name: Run Firebase App Testing Agent
        id: run_tests
        env:
          GOOGLE_APPLICATION_CREDENTIALS: sa.json
        run: |
          firebase apptesting:execute \
            --app=${{ secrets.FIREBASE_APP_ID }} \
            --test-dir=./tests \
            build/app/outputs/flutter-apk/app-debug.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: app-debug-apk
          path: example/build/app/outputs/flutter-apk/app-debug.apk
